services:
  keyway:
    image: ghcr.io/pesengineers/keyway:latest
    # For local development builds, uncomment:
    # build: .
    restart: unless-stopped
    env_file:
      - path: .env
        required: false
    volumes:
      - ./media:/media:ro
      - ./models:/models
      - ./output:/output
      - ./tmp:/tmp/keyway
    ports:
      - "127.0.0.1:8000:8000"
    # NVIDIA host: uncomment the next line. Select a specific GPU with
    # NVIDIA_VISIBLE_DEVICES in .env, preferably using its stable GPU UUID.
    # gpus: all
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    read_only: true
    tmpfs:
      - /run:size=16m,mode=0755

  # Optional local Ollama service for fully offline metadata review:
  # ollama:
  #   image: ollama/ollama:latest
  #   restart: unless-stopped
  #   volumes:
  #     - ./ollama:/root/.ollama
  #   ports:
  #     - "127.0.0.1:11434:11434"
  #   # For GPU acceleration in Ollama:
  #   # deploy:
  #   #   resources:
  #   #     reservations:
  #   #       devices:
  #   #         - driver: nvidia
  #   #           count: all
  #   #           capabilities: [gpu]
